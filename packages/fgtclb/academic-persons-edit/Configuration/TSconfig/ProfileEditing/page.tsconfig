# Compatibility Page TSconfig of the former ProfileEditing content element.
#
# This path remains as an inert source reference for installations migrating
# old records. It is not registered as a site set or selectable page TSconfig.
# It deliberately changes no backend configuration: the legacy CType stays
# hidden and InlineProfile is enabled only by its own component or by the
# aggregate configuration.
