# Aggregate page TSconfig: every component this extension ships.
#
# The counterpart of the set "fgtclb/academic-bite-jobs" for installations that
# include page TSconfig through the page field "Page TSconfig" instead of
# through a site set.
@import 'EXT:academic_bite_jobs/Configuration/TSconfig/List/page.tsconfig'
