A static template folder holding no TypoScript at all: no
"include_static_file.txt", no "constants" and no "setup" file in any of the
three suffixes TYPO3 reads. A TypoScript record including it gets nothing, and
that is what the configuration check reports.

The file is here because git does not track an empty folder.
