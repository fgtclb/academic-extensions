# This set deliberately carries no payload of its own.
#
# Its page TSconfig - the "academic" group header of the new content element
# wizard - is shipped by "Configuration/page.tsconfig" instead, which TYPO3
# auto-includes for the whole installation. The set used to import the same
# file a second time, which changed nothing (ACE-453).
#
# Do not remove the set: six other sets depend on it by name, and so may site
# configurations outside this repository.
name: fgtclb/academic-base-ctype-group
label: Academic Base - CType Group
