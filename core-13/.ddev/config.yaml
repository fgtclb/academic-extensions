name: core13-academics-v3
type: typo3
docroot: public
php_version: "8.2"
webserver_type: nginx-fpm
xdebug_enabled: false
additional_hostnames: []
additional_fqdns: []
# The instance runs on SQLite, seeded from "sqlite-databases/core-13.sqlite" by
# "config/system/additional.php". No database container is started at all - the
# "database" block below only satisfies DDEV's schema.
database:
    type: mariadb
    version: "11.8"
omit_containers: [db]
use_dns_when_possible: true
timezone: Europe/Berlin
composer_version: "2"
web_environment: []
corepack_enable: false
hooks:
    post-start:
        # Forward the host SSH agent first: "composer install" below resolves
        # packages that may come from private VCS remotes.
        - exec-host: ddev auth ssh
        - composer: install
