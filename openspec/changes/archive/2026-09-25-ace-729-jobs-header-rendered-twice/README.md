# ace-729-jobs-header-rendered-twice

The backport of the change of the same name on `main`: the job plugins render
the content element header only behind a per-extension switch, off by default,
so the header the content element layout renders appears once.
