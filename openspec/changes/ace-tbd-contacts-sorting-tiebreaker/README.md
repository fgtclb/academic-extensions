# ace-tbd-contacts-sorting-tiebreaker

The contacts of a page order by `sorting` alone; contacts sharing a value
follow the database. A `uid` tiebreaker makes the list the same on every
request, as rule 3 of the query rules asks.
